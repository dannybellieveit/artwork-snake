# Link Preview Feature Flag
# Controls whether the app tries to fetch actual filenames for link previews
#
# Values:
#   true (default) - Enable smart title fetching with multi-strategy fallback
#   false - Disable, use default "File Share" title
#
# For Cloudflare Pages:
#   Set in dashboard: Settings → Environment Variables → Production
#   Changes take effect in ~30 seconds after save
#   No code deployment needed for toggle
#
NEXT_PUBLIC_ENABLE_SMART_TITLES=true
